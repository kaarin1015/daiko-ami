[build]
  publish = "."
  functions = "netlify/functions"

# admin/site-data.json のキャッシュを無効化（常に最新を取得）
[[headers]]
  for = "/admin/site-data.json"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
